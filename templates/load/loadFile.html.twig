{% extends 'base.html.twig' %}

{% block title %}Home page{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn" onclick="alert('Process has been started!')">{{ button_label|default('Create') }}</button>
    {{ form_end(form) }}
{% endblock %}
